[pytype]

inputs = .
output = .pytype
exclude =
    build
    dist
    env
    .pytype
    examples
# Keep going past errors to analyse as many files as possible.
keep_going = True
python_version = 3.10
